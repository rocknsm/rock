[manager]
type=manager
host=localhost

[proxy-1]
type=proxy
host=localhost

[worker-1]
type=worker
host=localhost
interface=<%= node.run_state['monif'] %>
lb_method=pf_ring
lb_procs=<%= node.run_state['bro_cpu'] %>

