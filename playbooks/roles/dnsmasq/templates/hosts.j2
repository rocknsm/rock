#The syntax for the hosts file is:
#IP_address hostname [optional additional hostnames with spaces separators]
#Examples:
#1.2.3.4 myhost.domain myhost
#2.3.4.5 thishost

{% for host in groups['all'] %}
{{ hostvars[host].management_ipv4 }}  {{ hostvars[host].inventory_hostname }} {{ hostvars[host].inventory_hostname_short }}
{% endfor %}
