apiVersion: storage.k8s.io/v1beta1
kind: StorageClass
metadata:
  name: gluster-heketi
provisioner: kubernetes.io/glusterfs
parameters:
  resturl: "http://heketi_cluster_ip:8080"
  restuser: "user"
  restuserkey: "{{ heketi_user_key }}"
  volumeType: "none"
