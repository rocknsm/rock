apiVersion: v1
kind: Service
metadata:
  name: kafka
  labels:
    component: kafka
spec:
  selector:
    component: kafka
  ports:
  - name: transport
    port: {{ kafka_port }}
    targetPort: transport
