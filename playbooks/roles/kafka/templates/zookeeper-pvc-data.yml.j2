kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ zookeeper_pv_data }}
spec:
  storageClassName: gluster-heketi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ zookeeper_pv_size_mb }}Mi
