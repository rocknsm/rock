output {
  if "bro" in [tags] {
    # stdout { codec => rubydebug }
    elasticsearch {
      hosts => ["elasticsearch:9200"]
      index => "logstash-bro-%{+YYMMdd'h'HH}"
      # manage_template => false
      template_name => "logstash"
      template => "/usr/share/logstash/logstash-template.json"
      template_overwrite => true
    }
  }
}
