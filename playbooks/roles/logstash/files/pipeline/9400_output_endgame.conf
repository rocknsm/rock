output {
  if [event_type] == "endgame" {
    # stdout { codec => rubydebug }
    elasticsearch {
      hosts => elasticsearch
      index => "logstash-endgame-%{+YYMMdd'h'HH}"
      # manage_template => false
      template_name => "logstash"
      template => "/usr/share/logstash/logstash-template.json"
    }
  }
}
