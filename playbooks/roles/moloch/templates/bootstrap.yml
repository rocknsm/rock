apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: moloch-bootstrap
  labels:
    component: moloch
spec:
  replicas: 1
  selector:
    matchLabels:
     component: moloch
  template:
    metadata:
      labels:
        component: moloch
    spec:
      containers:
      - name: "moloch"
        image: "{{ moloch_image }}:{{ moloch_tag }}"
        volumeMounts:
        - name: config
          mountPath: {{ moloch_config_path }}/config.ini
          subPath: config.ini
      volumes:
      - name: config
        configMap:
          name: moloch
