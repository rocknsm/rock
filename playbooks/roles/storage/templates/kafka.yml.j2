{% for n in range(groups['sensors'] | length) %}
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: kafka-data-kafka-{{ n }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ kafka_pv_size }}Gi
  storageClassName: rbd
{% endfor %}